# T08 CI Build Task Memory

## Facts

- Task file: `.claude/tasks/T08-ci-build.md`
- Task branch: `feat/T08-ci-build`
- Dependency `T04` is satisfied by merged PR `#23`
- Linked issue: `#9`
- Recommended agent doc: `docs/agents/infra-engineer.md`

## Notes

- Start from a dedicated worktree at `../vridge-T08`
- Keep changes minimal and focused on build/publish CI only
- Make `NEXT_PUBLIC_APP_URL` explicit for both image build and runtime contract
- The code reality conflicts with older task notes: `backend/config/env.ts` still validates `NEXT_PUBLIC_*` server-side, so runtime injection cannot stay build-only
- Chosen approach: add GHCR build/publish workflow, validate it with a shell script, and inject runtime public env vars from `vridge-env` in Helm
- Local `helm` is unavailable in this shell, so chart render validation remains in `scripts/validate-vridge-chart.sh` but could not be executed here

## Unrelated Findings

- None yet
