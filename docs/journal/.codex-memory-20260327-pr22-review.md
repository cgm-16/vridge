# PR 22 review fix notes

- Date: 2026-03-27
- Goal: Read PR 22, address the latest `coderabbitai` review feedback, and fix the problem reported by GitHub Advanced Security.
- Starting branch: `dev`
- Current repo status at start: clean

## Constraints

- Keep changes minimal and scoped to the review/security findings.
- Use a dedicated feature branch/worktree before implementation.
- Record unrelated issues here instead of fixing them inline.

## Findings log

- Confirmed GitHub Advanced Security alert #4 on `.github/workflows/ci.yml:53-63`:
  - CodeQL rule `actions/missing-workflow-permissions`
  - Root cause: CI workflow omitted explicit `permissions`, leaving `GITHUB_TOKEN` scope implicit.
- Confirmed actionable CodeRabbit inline review on `deploy/helm/vridge/values.yaml`:
  - Root cause: container `securityContext` omitted `readOnlyRootFilesystem: true`.
- Confirmed low-risk Helm template improvements from the latest CodeRabbit summary:
  - `vridge.fullname` could render `vridge-vridge` when release name equals chart name.
  - Ingress host value was emitted without `quote`.
  - Probe rendering in `deployment.yaml` was repeated inline three times.
- Investigated CodeRabbit note about making `/readyz` and `/healthz` perform live dependency checks:
  - Not applied.
  - Reason: `prompt_plan.md` for T03 explicitly says readiness should stay lightweight and avoid live DB checks in phase 1 unless a strong built-in mechanism already exists.

## Applied fixes

- Added root-level `permissions: { contents: read }` to `.github/workflows/ci.yml`.
- Added `readOnlyRootFilesystem: true` to Helm `securityContext`.
- Added fullname de-dup logic to the Helm helper.
- Quoted ingress host rendering.
- Extracted repeated probe rendering into `vridge.probe`.
- Extended `scripts/validate-vridge-chart.sh` to assert:
  - quoted ingress host rendering,
  - `readOnlyRootFilesystem: true` in Deployment output,
  - no duplicate fullname when release name matches chart name.

## Verification

- `rg -n '^permissions:|contents: read' .github/workflows/ci.yml`
- `rg -n 'readOnlyRootFilesystem|define "vridge\\.probe"|host: \\{\\{ \\.host \\| quote \\}\\}|if eq \\.Release\\.Name \\$name' ...`
- `tmpdir=$(mktemp -d /tmp/helm-pr22.XXXXXX) && curl -fsSL https://get.helm.sh/helm-v3.15.4-darwin-arm64.tar.gz -o "$tmpdir/helm.tgz" && tar -xzf "$tmpdir/helm.tgz" -C "$tmpdir" && PATH="$tmpdir/darwin-arm64:$PATH" sh scripts/validate-vridge-chart.sh`
- `git diff --check`
