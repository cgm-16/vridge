# PR 23 Review Task Memory

- Date: 2026-03-27
- Goal: Read PR 23 and address its latest review comment.
- Constraints:
  - Current repository root is on `dev` with unrelated local changes; do not disturb them.
  - Prefer isolated worktree/branch handling before editing.
- Open questions:
  - None

## Findings

- PR 23 branch: `feat/T04-containerization`
- Existing clean worktree: `/Users/ori/repos/vridge/.worktrees/vridge-T04`
- Latest substantive review: `cgm-16` review submitted at `2026-03-27T02:09:43Z`
- Actionable points from that review:
  - Remove misleading `NEXT_PUBLIC_*` runtime env flags from `docker run` examples in `README.md` and `docs/handover.md`
  - Optional cleanup: avoid rebuilding the Better Auth Next.js handler on every request in `app/api/auth/[...all]/route.ts`

## Applied changes

- Added explicit build-time note for `NEXT_PUBLIC_*` in the production container sections of:
  - `README.md`
  - `docs/handover.md`
- Removed all `NEXT_PUBLIC_*` flags from both `docker run` examples.
- Hoisted the auth route handler to module scope in `app/api/auth/[...all]/route.ts`.

## CI failure follow-up

- Failing check: `ci` on GitHub Actions run `23627948077`
- Failure step: Docker builder stage at `Dockerfile:30` during `RUN pnpm build`
- Root cause:
  - `app/api/auth/[...all]/route.ts` was changed to create the Better Auth Next.js handler at module import time.
  - During `next build`, page data collection imports `/api/auth/[...all]`.
  - Docker builder stage only provides `NEXT_PUBLIC_*` build args, so `getAuth()` -> `getEnv()` throws for missing runtime secrets.
- Minimal fix strategy:
  - Keep handler caching, but make initialization lazy on first request instead of at module import time.
  - Replace the earlier route test with coverage that asserts:
    - import does not create auth handler
    - first request creates the handler
    - subsequent requests reuse it
