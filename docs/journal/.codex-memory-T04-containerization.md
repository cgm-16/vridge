# T04 containerization notes

- Task file: `.claude/tasks/T04-containerization.md`
- Branch: `feat/T04-containerization`
- Issue: `#5`
- Dependencies `T02` and `T03` are already merged into `dev`
- Local `dev` was behind `origin/dev` by 2 commits before preflight sync
- No `Dockerfile` or `.dockerignore` existed at start
- Existing production scripts in `package.json`: `build` = `next build`, `start` = `next start`
- Existing env validation in `backend/config/env.ts` already validates `NEXT_PUBLIC_APP_URL`
- Existing docs that may need alignment if behavior changes:
  - `docs/k3s-migration.md`
  - `docs/handover.md`
- Keep secrets out of image build, prefer multi-stage, run non-root if practical
