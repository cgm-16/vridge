# 2026-03-31 Obsidian Knowledge Base Build

## Goal

- Build an external Obsidian knowledge base for `vridge` under `/Users/ori/ori-knowldge-base/vridge`.
- Source of truth: `dev` baseline plus recent GitHub PRs/issues, repository docs, and durable `.codex-memory-*.md` notes.

## User Preferences

- Notes should be in English.
- Vault style should be light Obsidian, not Markdown-only and not heavily customized.
- Knowledge base should be an engineering brain, not a minimal onboarding guide.

## Constraints

- Target directory is outside current writable roots, so writing there will require escalated shell access.
- Avoid repo code/doc changes unrelated to this task.
- Keep the generated vault Markdown-first with minimal `.obsidian` config.

## Source Notes

- `origin/HEAD` points to `dev`.
- Current local checkout is `production`, but `origin/dev` and `origin/production` currently point to the same commit (`54123e4`) during planning.
- Recent merged PR cluster to capture:
  - #35, #37, #38, #41, #43, #45, #46
- Open docs/governance PR to mention as ongoing context:
  - #48
- Open workflow hazard issues to document:
  - #39, #40

## Durable Findings To Reuse

- Project is a Next.js 16 / React 19 / TypeScript ATS MVP with FSD frontend, layered backend, Better Auth, Prisma, Supabase/Postgres, and k3s/Helm deployment.
- Existing repo docs already summarize major routes, architecture, i18n, and current functionality.
- Prior memory note `.codex-memory-20260330-repo-scan.md` captures `.worktrees`-related lint/test failures and links them to GitHub issues #39 and #40.
