# Working Memory

- Task: port selected Claude workflow into Codex-native repo files.
- Requested scope:
  - Add Codex skills `start-task` and `finish-task`.
  - Add Codex agent prompt docs for `.claude/agents/scribe.md`, `.claude/agents/infra-engineer.md`, and `.claude/agents/app-developer.md`.
- Decisions locked with Ori:
  - Keep the wrap-up skill name as `finish-task`.
  - Represent agents as prompt docs only, not a custom agent runtime.
  - Adapt Claude workflow to `AGENTS.md` rules instead of mirroring it literally.
- Important repo rules to preserve:
  - Use `dev` as the base branch.
  - Prefer `git worktree` for task startup.
  - Keep issue open during PR review.
  - Use Korean Conventional Commit messages for commits.
  - `docs/agents/*` must be written in English.
- Existing source files:
  - `.claude/skills/start-task/SKILL.md`
  - `.claude/skills/finish-task/SKILL.md`
  - `.claude/agents/scribe.md`
  - `.claude/agents/infra-engineer.md`
  - `.claude/agents/app-developer.md`

- Task: PR 14 doc review follow-up in /private/tmp/vridge-codex-agent-prompts.
- Verification: `docs/agents/app-developer.md` and `docs/agents/infra-engineer.md` still only required Korean language for commits/PRs/issues; review comments remain valid on the PR branch.
- Decision: patch only those two docs in the PR worktree, align wording to `AGENTS.md`, and reference `docs/agents/scribe.md` from app-developer as requested.

- Task: PR 15 review follow-up in `/private/tmp/vridge-codex-task-skills`.
- Verification: `.agents/skills/finish-task/SKILL.md` still required `labels` and `milestone` in the extracted task fields while `.agents/skills/start-task/SKILL.md` did not.
- Decision: remove `labels` and `milestone` from `finish-task` step 2 and clarify the PR-body issue reference is conditional on `issue` being present.
- Result: pushed commit `b270031` to `origin/feat/codex-task-skills`, so PR 15 now reflects the schema alignment fix.

- Task: PR 15 review follow-up for `finish-task` commit flow.
- Verification: on `origin/feat/codex-task-skills`, `.agents/skills/finish-task/SKILL.md` still used `git status --short` and `git log --oneline origin/dev..HEAD`, then unconditionally proceeded to `git add -A`, so the empty-commit finding remained valid.
- Decision: create worktree `/private/tmp/vridge-pr15-finish-task-commit-guard` from `origin/feat/codex-task-skills` and patch only the control-flow text to distinguish worktree changes from ahead-of-dev commits before staging/committing.
- Result: pushed commit `9f67236` to `origin/feat/codex-task-skills`, so PR 15 now skips the commit step when the branch is already ahead of `dev` and the working tree is clean.
